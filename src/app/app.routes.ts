import { Routes } from '@angular/router';

export const routes: Routes = [
  // Routes seront ajoutées progressivement lors de la création des composants
  // Pour l'instant, l'application démarre avec AppComponent comme composant racine
];
